<%= form_for [@deliverable,@draft], html: { multipart: true } do |f| %>
  <%= f.label :attachment %>
  <%= f.file_field :attachment %>
  <%= f.text_field :description %>
  <%= f.submit "Submit Draft", class: "btn btn-primary" %>
<% end %>
